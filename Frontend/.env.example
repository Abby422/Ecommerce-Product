# Leave unset (or empty) to run the app in demo mode against bundled fixture data.
# Point it at a running Business Logic + Auth backend to use the real API.
# VITE_API_URL=http://localhost:5001
# VITE_AUTH_URL=http://localhost:5006
